#!/usr/bin/env python3
"""行2517-2575のパラメータ受け取りとJS import-events呼び出しを確認"""
APP = '/var/www/leaderboard/app.py'
with open(APP, encoding='utf-8') as f:
    lines = f.readlines()

# 1) 行2517-2575 (ルート関数の先頭 - パラメータ受け取り)
print("=== 行2517-2575: import-events 関数先頭 ===")
for i in range(2516, min(2575, len(lines))):
    print(f"{i+1:5}: {lines[i].rstrip()}")

# 2) JS: import-events を含む行（行数制限なし）
print("\n=== JS: import-events 全行 ===")
for i, line in enumerate(lines):
    ls = line.rstrip()
    if 'import-events' in ls and '@app.route' not in ls and 'def ' not in ls:
        for j in range(max(0,i-2), min(len(lines),i+4)):
            print(f"{j+1:5}: {lines[j].rstrip()}")
        print("---")
