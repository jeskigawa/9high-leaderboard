#!/usr/bin/env python3
"""import-eventsルートの中身とHTML/JSを確認"""
APP = '/var/www/leaderboard/app.py'
with open(APP, encoding='utf-8') as f:
    lines = f.readlines()

# 1) 行2517-2600 (import-eventsルート関数)
print("=== 行2517-2600: import-events ルート ===")
for i in range(2516, min(2600, len(lines))):
    print(f"{i+1:5}: {lines[i].rstrip()}")

# 2) import-events を含むJS行 (fetch/EventSource/URL)
print("\n=== JS: import-events 呼び出し ===")
for i, line in enumerate(lines):
    ls = line.rstrip()
    if 'import-events' in ls and i > 3300:
        for j in range(max(0,i-3), min(len(lines),i+6)):
            print(f"{j+1:5}: {lines[j].rstrip()}")
        print("---")
